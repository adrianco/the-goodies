sqlalchemy>=2.0.23
aiosqlite>=0.19.0
greenlet>=3.5.4
pydantic>=2.5.0
pydantic-settings>=2.14.2
# 0.133.0 is the first release to drop fastapi's starlette<1.0.0 cap. Needed so
# blowing-off's mcp>=2 resolves on Python >=3.14, where mcp requires
# starlette>=0.48 (on 3.11-3.13 mcp only needs starlette>=0.27, so this floor is
# about keeping the declared constraint set resolvable across python_requires).
fastapi>=0.133.0
click>=8.1.7
python-dateutil>=2.8.2
uvicorn[standard]>=0.24.0
httpx>=0.25.2
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
argon2-cffi>=25.1.0
pyjwt>=2.13.0
qrcode>=8.2
pillow>=12.3.0
apscheduler>=3.11.3

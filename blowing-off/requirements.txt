inbetweenies
sqlalchemy>=2.0.51
greenlet>=3.5.4
aiosqlite>=0.22.1
httpx>=0.24.0
aiohttp>=3.14.3
rich>=10.0.0
click>=8.3.3
tabulate>=0.10.0
# blowingoff/mcp/server.py targets the 2.x API: Tool.input_schema (was
# inputSchema), keyword-only Server(version=...), and the on_list_tools /
# on_call_tool constructor callbacks that replaced the removed
# @server.list_tools() / @server.call_tool() decorators.
# On Python >=3.14 mcp 2.x requires starlette>=0.48, which older fastapi pins
# forbid; hence funkygibbon's fastapi>=0.133.0 floor.
mcp>=2.0.0,<3
pytest>=7.0.0
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
